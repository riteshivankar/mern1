<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>First Task.Project</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <style src="style.css" href="/stylesheet/style.css"></style>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input type="text" class="block w-full rounded-md outline-none  px-5 py-2 bg-zinc-800" placeholder="your title goes here" name="title">
                <textarea class="block mt-2 w-full rounded-md outline-none px-5 py-2 resize-none bg-zinc-800"  name="details" id="" placeholder="Details about task"></textarea>
                <input class="mt-2 px-5 py-2 bg-blue-600 rounded-md" type="submit" placeholder="create task">
            </form>
            <div class="tasks flex gap-3 flex-wrap p-10">
                        <% if(files.length >0){ %>
                            <% files.forEach(function(val){ %>
                        <div class="task  w-72 px-3 py-4 bg-zinc-800 rounded-md">
                            <h1 class="text-white tracking-tighter text-3xl "><%= val %></h1>
                            <div class="flex w-full justify-around items-center mt-3 ">
                                <a class="text-blue-600 inline " href="/files/<%= val %>"> Read more</a>
                                <a class="text-zinc-600" href="/edit/<%= val%>">edit</a>
                        

                            </div>
                        </div>
                <% })%>

           <% } else { %>
            <h3 class="text-zinc-600  text-2xl">No Task Yet</h3>

           <% }%>


               
            </div>
        </div>
    </div>
</body>
</html>